# Development / optional extras for local development and UI
# Install these when working on the project locally or when running the
# Streamlit annotation UI.

streamlit>=1.0
pandas>=1.5

# Common dev tools
pytest>=7.0
pre-commit>=3.0

{
  "$schema": "https://railway.app/npm/railway.schema.json",
  "build": {
    "builder": "DOCKER",
    "dockerfilePath": "Dockerfile",
    "context": "."
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "rootDirectory": "backend"
  }
}

This folder contains archived/experimental files from the `routemaster_agent` package.

Moved items:
- `run_test_runner_debug.py` — lightweight debug script for TestRunner (archived on cleanup).

Keep archived files for historical reference; they are not imported by the package.
